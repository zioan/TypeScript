const userName = 'Ioan';
console.log('from app.ts file...');

//To watch a single TS file for changes
// tsc app.ts -w

//To watch an entire TS project for changes
// tsc --init (only one time)
// tsc -w
// npm start (if a live server is installed -> see package.json)
